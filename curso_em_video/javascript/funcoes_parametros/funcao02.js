function soma(n1=0, n2=0) { //(parâmetros pré definidos) caso algum parâmetro não seja passado, ira considera que é igual a 0, isso evitará que o retorno seja NaN.
    return n1 + n2
}
console.log(soma(5))